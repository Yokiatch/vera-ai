# .github/workflows/keep-alive.yml
name: Keep Vera Alive
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl https://vera-ai-6anh.onrender.com/v1/healthz
